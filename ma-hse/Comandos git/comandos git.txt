cd C:\HSE\ma-hse
git status
git add .
git commit -m "Correção Deploy Automatico2"
git switch -c correcoesluis
git push origin correcoesluis
$env:Path += ";C:\Program Files\GitHub CLI"
gh pr create --base main --head correcoesluis --title "Correções Luis" --body "Correções aplicadas"
gh pr merge --auto --merge --delete-branch

Executar para fazer o delpoy

ssh ma_hse_deploy@157.245.31.71
sudo /usr/local/sbin/deploy-ma-hse

Teste finsl

cd C:\HSE\ma-hse
docker compose up -d
npm run dev:all




user: admin@maxsafety.com
palavra-passe: maXSafety


Em produção, com N0_ADMIN_EMAIL=admin@maxsafety.com, 
a ensureDefaultAdminUser reconhecerá o email do login e criará o utilizador automaticamente com a password de SEED_DEFAULT_PASSWORD

# Seed defaults
N0_ADMIN_EMAIL=admin@maxsafety.com
SEED_DEFAULT_PASSWORD=ChangeMe123!



N0_ADMIN_EMAIL	admin@maxsafety.com
SEED_DEFAULT_PASSWORD	maXSafety



- N0 na DB: admin@maxsafety.com (password: ChangeMe123! local / maXSafety produção)

131/131 testes a passar, zero erros de tipo.
Em produção, quando fizeres deploy, o seed vai criar o N0 com admin@maxsafety.com / maXSafety (desde que o .env.production esteja configurado com essas env vars).